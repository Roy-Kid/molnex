# Architecture Blueprint

> 跑 `/mol:map` 填充本蓝图 / Run `/mol:map` to populate this blueprint.

Stub — the `librarian` agent reads this file during `/mol:spec` Step 4.5 to find
reuse candidates and recommended placement. Populated by `/mol:map`; never edited
by hand.
