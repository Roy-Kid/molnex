# MolNex — Evolving Decisions

Capture non-obvious decisions, trade-offs, and provisional rules here via `/mn-note`.
When an entry stabilises, `/mn-note` promotes it into `CLAUDE.md` and deletes it here.

This file is read by every `mn-*` agent before running checks.

---

<!-- Entries below. Format:
## <short title>  (YYYY-MM-DD)
**Context.** why this came up.
**Decision.** what we chose.
**Status.** provisional | promoting | stable.
-->
