"""Brute-force NumPy oracles for parity testing.

Modules here are intentionally minimal, dependency-free (NumPy only),
and self-documenting — they exist so production-side parity tests can
be written against textbook formulae directly without pulling in the
upstream LES git package as a dev dependency.
"""
